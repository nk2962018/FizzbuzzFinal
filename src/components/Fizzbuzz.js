import Button from "./Button";

const Fizzbuzz = () => {
return (
    <div data-testid="fizzBuzz">
        <Button
         text="Increment"
        />
         <Button
         text="Decrement"
        />
    </div>
  );
};

export default Fizzbuzz;
